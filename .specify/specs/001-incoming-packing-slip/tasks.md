# Spec 001 — Tasks

- [x] Document live 3-step UX
- [x] Document `/api/health`, `/api/extract-po`, `/api/project-name/:n`, `/api/upload`
- [x] Document live PO regex and filename template
- [x] Record notes-not-uploaded gap
- [ ] Owner decision: adopt live vs `lib/file-naming.js` filename
- [ ] Owner decision: adopt live vs `ocr-patterns.js` PO parsing
- [ ] Fix notes field transmission (code — out of kit-only pass)
- [ ] Remove hardcoded FILE_SERVICE_* fallbacks (code — separate)
