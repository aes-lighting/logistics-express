# Spec 002 — Plan

Deferred until MVP is defined.

1. Keep ontology DeferredDomain classes accurate to `lib/`.
2. Do not restore routes in this Spec Kit pass.
3. When MVP includes platform features, create a promotion build with deps + `server.js` wiring + tests.
