# spec-kit (human layer)

**Non-binding.** Binding law lives in [`.specify/spec.md`](../.specify/spec.md).

This folder mirrors the my-accounting-v4 split: human-readable architecture notes that defer to the Spec Kit.
